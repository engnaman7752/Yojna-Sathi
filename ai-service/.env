MODEL_PROVIDER=bedrock
BEDROCK_CHAT_MODEL_ID=anthropic.claude-3-sonnet-20240229-v1:0
AWS_REGION=ap-south-1
BACKEND_URL=http://localhost:8081

# Note: Ensure your AWS_ACCESS_KEY_ID and AWS_SECRET_ACCESS_KEY are either set here 
# or configured locally via `aws configure` so boto3 can authenticate with AWS.
